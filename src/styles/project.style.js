import styled from "styled-components";

export const StyledProject = styled.section`
  padding: 0 var(--padding-body) var(--padding-body-sm);
  text-align: center;
  h2 {
    text-align: start;
    color: var(--purple);
    font-size: var(--fs-sm);
    padding-top: var(--padding-subject);
  }
`;
