import styled from "styled-components";

export const StyledAbout = styled.div`
  padding-inline: 16.67vw;
  height: 100vh;

  section {
    color: var(--white);
    padding-block: 20px;
    display: flex;

    div {
        width: 50%;
    }
  }
`;
